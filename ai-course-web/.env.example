# Keep this file as a template only. Never commit a real key.
GEMINI_API_KEY=
# Confirm availability in your Google AI Studio project before enabling production use.
GEMINI_MODEL=gemini-3.1-flash-lite
GEMINI_EVALUATION_MOCK=false
# The static preview has no Cognito session. Do not use it as production authentication.
COURSE_AUTH_MODE=development
